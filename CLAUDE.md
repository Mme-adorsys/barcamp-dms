# CLAUDE.md

# Additional Instructions
- @docs/gtd_board_guide.md

This file provides guidance to Claude Code (claude.ai/code) when working with code in this repository.

## Project Overview

This appears to be a new project named "dms" located in an IntelliJ IDEA projects directory. The project structure and technology stack are not yet established.

## Development Setup

Since this is a new project, initial setup will depend on the chosen technology stack. Common patterns for projects in IntelliJ IDEA include:

- **Java/Kotlin projects**: Look for `build.gradle`, `pom.xml`, or similar build files
- **JavaScript/TypeScript projects**: Look for `package.json` and use `npm` or `yarn` commands
- **Python projects**: Look for `requirements.txt`, `pyproject.toml`, or similar dependency files

## Common Commands

Commands will be added as the project structure becomes established. Typical patterns include:

- Build: `./gradlew build` (Gradle) or `mvn compile` (Maven) or `npm run build` (Node.js)
- Test: `./gradlew test` (Gradle) or `mvn test` (Maven) or `npm test` (Node.js)
- Run: Depends on project type and main class/entry point

## Architecture Notes

Project architecture will be documented here as the codebase develops. Key areas to cover:

- Main application entry points
- Core business logic organization
- Data persistence approach
- External integrations
- Configuration management

## Development Notes

- This project is currently empty - structure and conventions will be established as development progresses
- Consider adding linting/formatting tools appropriate for the chosen technology stack
- Establish testing framework and conventions early in development
